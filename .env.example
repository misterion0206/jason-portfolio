# Google Gemini API key (server-side only — never expose to the browser).
# Free tier is sufficient; the chatbot uses gemini-2.5-flash-lite.
GEMINI_API_KEY=

# Upstash Redis (IP rate limiting for /api/chat)
KV_REST_API_URL=
KV_REST_API_TOKEN=
